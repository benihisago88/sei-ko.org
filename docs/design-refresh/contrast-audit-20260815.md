# コントラスト改善の検証メモ（2026-08-15）

## 変更内容

- `styles.css` に `--accent-strong: #0d7485` を追加し、小文字の補助ラベル、選択済みタブ、サービス番号、タイムライン、リンク、フォームフォーカス、CTAの文字色／背景色を役割トークンへ集約した。
- 白文字を使う主要CTAとヒーローの強調背景を `--accent-strong` に変更し、白との可読性を確保した。
- ガイドのパンくず、記事CTA内のアイブラウ、プライバシーポリシーのリンク・改定日を、背景に対して十分なコントラストとなる色へ変更した。
- 4ページのCSSキャッシュバスターを `?v=20260815.1` に統一した。

## 検証結果

| 項目 | 結果 |
|---|---|
| Lighthouse Accessibility（トップ） | 100 |
| Lighthouse Accessibility（ガイド） | 100 |
| Lighthouse Accessibility（プライバシー） | 100 |
| Lighthouse Accessibility（404） | 100 |
| トップのレイアウト | 横スクロールなし（`scrollWidth === clientWidth`） |
| 読込資産 | `styles.css?v=20260815.1` と `script.js?v=20260810.2` が正常取得 |
| インラインCSS／実行型インラインJS | 本修正では追加なし |
| `git diff --check` | 通過 |

ローカルの`python -m http.server`は`.htaccess`を解釈しないため、CSP・長期キャッシュ・圧縮・リダイレクトについては本番環境で別途確認が必要である。

## 画面確認

| 画面幅 | 確認結果 |
|---|---|
| 375px | ヒーローの強調背景、主CTA、アイブラウ、信頼性インジケーターが十分な濃度で表示され、レイアウト崩れや横方向のはみ出しは見られない。 |
| 1280px | ナビゲーション、ヒーローの強調背景、主CTA、写真、課題提起セクションの文字階層が自然に表示され、配色変更によるレイアウト影響は見られない。 |
